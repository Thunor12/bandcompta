[package]
name = "bandcompta-shared"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.219", features = ["derive"] }

[target.'cfg(target_arch = "wasm32")'.dependencies]
gloo-net = "0.5.0"
